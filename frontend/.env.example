# Where the FastAPI backend is reachable from the browser.
#
# NEXT_PUBLIC_ is mandatory: this value is read in client components, and Next
# strips anything without that prefix out of the browser bundle.
NEXT_PUBLIC_API_URL=http://localhost:8000
